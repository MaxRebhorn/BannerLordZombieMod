bin/
obj/
*.user
.vs/
*.log
*.dmp
*.suo

# SteamCMD downloads its own runtime + your personal Steam userdata cache
# here on first run - never commit this, it's local tooling, not source.
tools/steamcmd/
