# Zombie Plague

Generated by BannerlordSage for Bannerlord mod authors who want a compilable starting point instead of a blank folder.

## Build

```powershell
dotnet build ".\src\ZombiePlague.csproj" -c Release
```

The output DLL is written to `bin\Win64_Shipping_Client\ZombiePlague.dll`, which already matches `SubModule.xml`.

## Generated Features

- Namespace: `ZombiePlague`
- Authors: `maxrebhorn01`
- Target framework: `netstandard2.0`
- Campaign behavior scaffold: `true`
- Harmony example scaffold: `false`
- ButterLib wiring: `false`
- UIExtenderEx wiring: `false`
- MCM wiring: `false`

## Declared Dependencies

- Native:v1.4.8.*
- SandBoxCore:v1.4.8.*
- Sandbox:v1.4.8.*
- StoryMode:v1.4.8.*

## Notes

- `SubModule.xml` is emitted in the modern community style with `DependedModuleMetadatas`.
- Localization tokens live in `ModuleData\Languages\std_module_strings_xml.xml`.
- If your Bannerlord install path changes later, update the `HintPath` entries in `src\ZombiePlague.csproj`.

## Generator Warnings

- Harmony support requires 'Bannerlord.Harmony', but that module was not found under the local Bannerlord Modules directory.
- Harmony example was requested but Bannerlord.Harmony is not installed. The patch scaffold was skipped.
